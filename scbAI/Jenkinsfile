pipeline { 
    agent any  
    environment {
        def mvn = "/home/pi/apache-maven/bin/mvn"
    }
    stages { 
        stage(' Deploy') { 
            steps { 
				sh "rm -rf /home/pi/scbAI/*"
				sh "cp -R ${WORSPACE}/scbAI/* /home/pi/scbAI"
			}
        }
    }
}